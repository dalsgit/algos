package dataStructures;

public class LinkedList {
    Node head;

    public Node insert(int data, int locationIdx) {
        Node newNode = new Node(data);
        if locationIdx == -1 {
            // add at the end
            
        }
    }
}

public static void main(String args[]) {

}